# WP-0002 first-playable ratification proposal

Status: **A0 proposal / unsealed / non-executable**
Prepared from protected main: `b6b283fd63ab54fed5cd9b6dc6ac78a166cc5bb5`

This is the smallest proposed governance transaction after the completed
WP-0003 technical foundation. It does not itself release WP-0003, ratify an
open decision, accept or activate WP-0002, or authorize gameplay work.

## Proposed transaction

1. Creator-accept WP-0003 completion at immutable contract
   `fb8b7362f3c6915447964e134486123ede6d8b22430fe744f5c0a03c0537bd47`
   under claim `ACCEPT-COMPLETION-WP-0003`, then materialize its existing
   evidence-backed lifecycle as released. The distinct Stage-B completion
   receipt is the dependency authority; the contract-bound visual-evidence
   list stays unchanged, while the existing screenshot and validation manifest
   remain only in WP-0003's mutable evidence manifest.
2. Ratify these reversible recommended defaults for WP-0002: `DRIVE` (direct,
   weighty arcade control), `ROUTE+ROAD` (route graph plus compact authored
   road), `SLOWED`, `SCARS`, `CULTURES`, `ACCENT`, `SOLO-OFFLINE`,
   `RATIFY-THESIS`, `RATIFY-CORE`, and `RATIFY-SLICE`. Each remains
   supersedable by a later creator decision.
3. Reaffirm `TITLE-AND-PROTAGONIST-SASHA` and
   `COLONY-HUMANS-ROBOTS-OR-MIXED` through two new superseding decision heads,
   each with its own sealed creator-issued `decision-ratification` receipt,
   external-protected resolver, and exact WP-0002 contract binding. The old
   unsealed local-git-tree `RR-CREATOR-20260715-02` remains historical source
   evidence only. Gate receipt IDs remain null until those distinct heads and
   receipts genuinely exist, then materialization updates them to the new IDs.
4. Issue `AUTHORIZE-CITY-COMPARISON` only as a distinct sealed
   `creator-authorization` receipt with creator issuer, external-protected
   resolver, and exact WP-0002 contract binding. D-0030 remains open until the
   comparison selects and separately ratifies a city grammar.
5. Do not ratify D-0039. Human-only, robot-only, and mixed colonies use the
   same golden path; differentiated mechanics run only after a separate
   `MECHANICAL-COMPOSITIONS` receipt.
6. Accept proposed WP-0002 immutable contract
   `50565d914f463c39e502600dc370142a7449c695fb558c5da03cfd3e1abb13a3`.
7. Approve `A1B-WP-0002-LOCAL-DEV` proposal
   `ce6375756cf1a5589fbbcd67ed201b74ba6b4afdf0f6512894ae1eb991bd9a27`
   as the successor policy: fresh protected-main `agent/*` branches, protected
   PRs plus deterministic `validate` and `wp0002-core`. The base-owned
   `wp0002-policy` pull-request-target workflow is a candidate-independent
   canary only: it becomes a required context only after a live Stage-B PR
   proves that GitHub attaches it to the latest head. Until then, and whenever
   that attachment is absent, every PR requires transparently attributed
   creator-delegated manual release transmitted by Codex; autonomous
   auto-merge is disabled. Cursor Approval Agent (app `1210556`) is optional
   independent AI review, carries candidate/policy prompt-injection residual,
   and is neither deterministic validation nor a blocking authority. Foundation,
   governance, and receipts read-only during implementation; only
   `Unity_ReadConsole`, `Unity_RunCommand`, `Unity_ManageEditor`,
   `Unity_ManageGameObject`, and `Unity_Camera_Capture`; no direct Unity
   process, install/dependency, credential, publishing, protection-bypass, or
   direct-merge authority. The five-tool UI is not an OS sandbox:
   `Unity_RunCommand` can execute arbitrary Editor C# with host filesystem,
   network, process, and package reach, so direct use remains prohibited until
   the enumerated content-addressed WP-0002 gate dispatcher is materialized;
   every call must log exact source hash, command, and result, and unattended
   third-party use requires a future narrow `Unity_WP0002_RunGate` tool. The
   only dependency/package-graph exception is to
   link the existing `com.ac21.sasha.simulation-core` and
   `com.ac21.sasha.save-contracts` siblings through exact `file:../../SimulationCore`
   and `file:../../SaveContracts` entries resolved relative to
   `Game/Packages/manifest.json`, with writes limited to
   `Game/Packages/manifest.json`, `Game/Packages/packages-lock.json`, and the
   separately declared build-settings path. `SimulationCore/package.json` and
   `SaveContracts/package.json`, both package READMEs, all existing package
   metadata, the entry/package/policy/scope validators, and both WP-0002
   workflows remain read-only and byte-identical to protected
   base; a required package-graph security check rejects every other version,
   source, URL, depth, dependency-map, registry, network, Git, tarball, embedded
   copy, or external-dependency change. The entry-gate checker, package-graph
   checker, and `wp0002-ci` workflow are already materialized, byte-pinned, and
   protected read-only. The already-open canonical Unity target is retained;
   full-worktree clean remains false solely for the exact creator-owned drift
   `.codex/config.toml`, `Game/ProjectSettings/ProjectSettings.asset`, and
   `Game/ProjectSettings/SceneTemplateSettings.json`, while the index,
   non-excluded scope, and reserved scope must be clean and a content-addressed
   attestation capture must bind raw porcelain-v2 bytes plus metadata-only file
   observations without retaining creator file contents. A separate live
   GitHub protection capture binds exact contexts/app IDs and the policy-canary
   result. The boundary remains
   proposed until a held exact reservation and distinct protected activation
   receipt replace its null observation and authority fields.
8. Authorize only disposable local profile `last-bearing-dev-v1`, with atomic
   new-generation publication beneath the fixed
   `UnityEngine.Application.persistentDataPath/last-bearing-dev-v1` child,
   Unity-runtime writes only through SaveContracts, immediately preceding
   verified generation kept as last-good, current/last-good-only loads,
   fail-closed reads, no host/agent writes or sibling scans, and no migration or
   compatibility promise.
9. Acknowledge that this delegated Codex session has authenticated repository
   admin capability through the creator-owned account. Branch protection is
   strong merge-process enforcement, not credential isolation; bypass remains
   denied creator policy backed by live audit/canary evidence, not a
   cryptographically unavailable operator capability. Acceptance, activation,
   and release authority must reference an authenticated AC-21 GitHub
   transaction with actor, exact body hash, commit, timestamp, and delegated
   attribution; a locally authored receipt alone is not non-forgeable proof.

## Compact creator sentence

> I ratify the WP-0002 first-playable proposal at SHA-256 `<PROPOSAL-SHA256>`: completion claim `ACCEPT-COMPLETION-WP-0003`; decision ratifications `DRIVE`, `ROUTE+ROAD`, `SLOWED`, `SCARS`, `CULTURES`, `ACCENT`, `SOLO-OFFLINE`, `RATIFY-THESIS`, `RATIFY-CORE`, `RATIFY-SLICE`, `TITLE-AND-PROTAGONIST-SASHA`, and `COLONY-HUMANS-ROBOTS-OR-MIXED`; separately, creator authorization `AUTHORIZE-CITY-COMPARISON` while D-0030 remains open; `ACCEPT-WP-0002` at exact contract `50565d914f463c39e502600dc370142a7449c695fb558c5da03cfd3e1abb13a3`; and proposed creator-delegated protected-PR boundary `ce6375756cf1a5589fbbcd67ed201b74ba6b4afdf0f6512894ae1eb991bd9a27`, including exact `validate` plus `wp0002-core`, contingent `wp0002-policy` only after a live latest-head canary, optional nonblocking Cursor AI review with prompt-injection residual, creator-delegated per-PR release when the canary is absent, the exact five-tool Unity MCP allowlist with acknowledged policy-only `Unity_RunCommand` containment and enumerated hash-bound dispatcher-before-use, protected materialized self-verification, read-only foundation/governance/receipts/package READMEs, protected-main `agent/*` branches, only `file:../../SimulationCore` and `file:../../SaveContracts` resolved from `Game/Packages/manifest.json`, exact excluded creator-owned drift `.codex/config.toml`, `Game/ProjectSettings/ProjectSettings.asset`, and `Game/ProjectSettings/SceneTemplateSettings.json` with clean index/non-excluded/reserved scopes and metadata-only raw capture, fixed-child Unity-runtime-only disposable atomic `last-bearing-dev-v1`, acknowledged repo-admin/branch-protection residual with denied bypass policy and authenticated GitHub transaction evidence, and no D-0039 mechanics claim.

One authenticated source may support distinct completion, decision-ratification,
creator-authorization, packet-acceptance, and packet-activation receipt JSONs,
but their kinds and IDs remain distinct. WP-0002 activation remains a
later distinct protected receipt bound to the finalized held-reservation
boundary bytes; this sentence does not fabricate that runtime attestation.
